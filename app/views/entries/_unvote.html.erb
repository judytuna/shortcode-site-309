<%= form_for current_user.votes.find_by_entry_id(@entry),
             :html => { :method => :delete },
             :remote => true do |f| %>
  <div class="actions"><%= f.submit "Remove vote" %></div>
<% end %>
