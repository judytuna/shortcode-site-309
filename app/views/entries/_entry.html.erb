<tr>
  <td class="entry">
    <h2><span class="title">
      <%= link_to entry.title, entry_path(entry) %>
    </span></h2>
    
    <form id="render_<%= entry.image_name %>"
    	name="render_job_form"
    	action="<%= entry.povserver + 'cgi-bin/request.py' %>"
    	method="post">
    	<input type="hidden" name="code"
    		value='<%= entry.shortcode %>' />
    	<input type="hidden" name="name"
	    	value=<%= entry.image_name %> />
    </form>
    
    <nav>
    <%= link_to "edit", edit_entry_path(entry) %> |
    <%= link_to "show", entry_path(entry) %> |
    <a href = "javascript: document.getElementById('render_<%= entry.image_name %>').submit()"> render </a>
    </nav>
    
    <%= image_tag entry.povserver + "images/" + entry.image_name + '.png' %>
    
    <br/>

    <br/>
    
    <label>Short Code</label>
    <pre class="shortcode"><%= entry.shortcode %></pre>
    
    <% if entry.longcode? %>
      <label>Long Code</label>
      <pre class="longcode"><%= entry.longcode %></pre>
    <% end %>
    
    <% if entry.comments? %>
	  <label>Comments</label>
	  <pre class="comments"><%= entry.comments %></pre>
    <% end %>

    <span class="timestamp">
      Posted <%= time_ago_in_words(entry.created_at) %> ago.
    </span>
    
  </td>
</tr>
