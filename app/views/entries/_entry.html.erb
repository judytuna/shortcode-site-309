<tr>
  <td class="entry">
    <h2><span class="title">
      <%= link_to entry.title, entry_path(entry) %>
    </span></h2>
    
    <nav>
    <%= link_to "edit", edit_entry_path(entry) %> | 
    <%= link_to "show", entry_path(entry) %>
    </nav>
    
    <br />
    <!-- testing Paperclip -->
    <% unless entry.picture == nil %>
      <%= image_tag entry.picture.url %>
      <%= image_tag entry.picture.url(:medium) %>
      <%= image_tag entry.picture.url(:thumb) %>
    <% end %>
    
    <br/>
    
    
    <label>Short Code</label>
    <pre class="shortcode"><%= entry.shortcode %></pre>
    
    <% if entry.longcode? %>
      <label>Long Code</label>
      <pre class="longcode"><%= entry.longcode %></pre>
    <% end %>
    
    <% if entry.comments? %>
	  <label>Comments</label>
	  <pre class="comments"><%= entry.comments %></pre>
    <% end %>

    <span class="timestamp">
      Posted <%= time_ago_in_words(entry.created_at) %> ago.
    </span>
    
  </td>
</tr>
